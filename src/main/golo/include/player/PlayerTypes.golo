module audiostreamerscrobbler.player

# Modules should probably register player types dynamically at some point, but let's not overengineer in this early stage... :-)

union PlayerTypes = {
	BLUESOUND
}